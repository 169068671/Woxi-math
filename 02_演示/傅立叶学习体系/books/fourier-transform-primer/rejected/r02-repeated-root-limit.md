# 淘汰：合并根的极限构造法

- 来源：`f11` / `c13` / `ce11` / `ce12`
- V1：不通过。视频只在二阶常系数方程重根这一处完整使用该方法，没有第二个独立对象或章节的佐证。
- V2：通过。可外推到参数合并时构造第二个独立方向的问题。
- V3：通过。用差商极限解释 `t e^{st}` 不是普通常识。
- 处理：不独立成 skill；作为 `v07 用特征函数代数化线性算子` 的高价值案例和边界测试保留。
