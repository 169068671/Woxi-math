# 降级：把“函数是无穷维向量”单独做成 skill

- 来源：`f03` / `p04`
- V1：通过。该视角贯穿函数内积、线性算子和积分变换。
- V2：不通过（作为孤立口号）。若不附带“如何迁移向量结构”的步骤，只能产生类比，不能稳定解答新问题。
- V3：边界通过，但原子性不足。
- 处理：不独立成 skill；并入 `v02 从有限维结构迁移到函数空间`，保留其可执行映射表和适用条件。
