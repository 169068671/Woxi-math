# 降级：以单位周期归一化投影

- 来源：`f13` / `p17` / `ce14`
- V1：不通过。视频只在傅立叶级数系数处直接展开 `1/T` 的理由。
- V2：通过。能外推到任意重复区间积分的平均化问题。
- V3：不通过独立性。它是一般“基归一化/除以范数平方”规则的具体实例，单独做 skill 会与基审计和傅立叶级数重复。
- 处理：并入 `v09` 与 `v10`，反例继续用于压力测试。
